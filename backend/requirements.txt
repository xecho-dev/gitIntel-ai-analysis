# ─── 依赖说明 ────────────────────────────────────────────────────────────
# ⚠️ protobuf 版本冲突警告：
#   dashvector 要求 protobuf<6.0
#   langgraph 要求 protobuf>=6.31.1
# 解决：先安装 langgraph，再单独装 dashvector 并忽略 protobuf 冲突
# 推荐安装顺序：
#   pip install -r requirements.txt --no-deps
#   pip install dashvector  # 自动拉取兼容版本的 protobuf
#   pip install langgraph langgraph-api langchain-core langchain-openai
# ─────────────────────────────────────────────────────────────────────

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-openai>=0.3.0
langchain-anthropic>=0.3.0
httpx>=0.27.0
pydantic>=2.0.0
python-dotenv>=1.0.0
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
supabase>=2.0.0
gitpython>=3.1.0
tree-sitter>=0.23.0,<0.25
tree-sitter-python>=0.23.0,<0.25
tree-sitter-languages>=1.10.0,<1.11
aiofiles>=24.0.0
reportlab>=4.0.0
dashvector>=1.0.0,<2.0.0
dashscope>=1.20.0
lizard>=1.17.0