# ============================================================
# GitIntel 后端环境变量配置
# ============================================================
# 复制此文件为 .env，填入实际值
# 路径：/opt/gitintel/.env

# ========== API 密钥 ==========
# OpenAI API Key（Agent 层调用 LLM）
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# OpenAI API 基础地址（可选，默认官方接口）
OPENAI_BASE_URL=https://api.openai.com/v1

# ========== PostgreSQL ==========
# 格式: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://gitintel:password@your-pg-host:5432/gitintel

# ========== 服务器配置 ==========
# 后端监听端口（保持默认 8000）
PORT=8000

# 前端地址（用于 CORS 白名单）
# 如果前端使用域名，填入完整 URL
# 如果只有 IP，填入 http://IP:端口
FRONTEND_URL=https://your-domain.com

# ========== NextAuth 认证 ==========
# NextAuth JWT 签名密钥（必须与前端 AUTH_SECRET 一致）
# 用于后端解码前端传来的 NextAuth Session Token
AUTH_SECRET=your-nextauth-secret-at-least-32-chars

# ========== 可选功能 ==========
# GitHub Token（访问私有仓库，用于代码分析）
# 在 GitHub Settings > Developer settings > Personal access tokens 生成
# 需要 repo 范围的读取权限
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
