---
description: 项目整体架构与目录结构规范
alwaysApply: true
---

# GitIntel 项目架构规范

## 技术架构

```
┌──────────────────────────────────────────────────────────────┐
│                        前端 (Next.js)                        │
│              Next.js App Router + Tailwind CSS               │
│                     Zustand 状态管理                         │
│                      端口: 3000                              │
└─────────────────────────┬────────────────────────────────────┘
                          │ HTTP / SSE 流式响应
                          ▼
┌──────────────────────────────────────────────────────────────┐
│                  BFF 层 (Next.js Route Handler)               │
│               frontend/app/api/**/route.ts                    │
└─────────────────────────┬────────────────────────────────────┘
                          │ HTTP
                          ▼
┌──────────────────────────────────────────────────────────────┐
│                    Agent 层 (FastAPI)                         │
│              FastAPI + LangGraph + SSE                       │
│                    端口: 8000                                │
└──────────────────────────────────────────────────────────────┘
```

## Monorepo 目录结构

```
gitintel-ai-analysis/          # 根目录 (pnpm workspace)
│
├── frontend/                   # Next.js 前端（独立部署到 Vercel）
│   ├── app/                    # Next.js App Router
│   │   ├── page.tsx            # 主页面
│   │   ├── layout.tsx          # 根布局
│   │   ├── globals.css
│   │   └── api/                # BFF Route Handler（代理 Agent 层）
│   │       └── analyze/
│   │           └── route.ts
│   ├── components/             # React 组件
│   │   ├── agents/             # Agent 结果展示组件
│   │   └── ui/                 # 通用 UI 组件
│   ├── lib/                    # 前端工具库
│   │   ├── utils.ts
│   │   └── api.ts              # 统一 API 调用封装（含 SSE 消费）
│   ├── store/                  # Zustand 状态管理
│   │   └── useAppStore.ts
│   ├── package.json
│   ├── tailwind.config.ts
│   ├── next.config.ts
│   └── tsconfig.json
│
├── backend/                    # FastAPI Agent 层（独立部署）
│   ├── agents/                 # Agent 实现
│   │   ├── __init__.py
│   │   ├── base_agent.py       # Agent 基类
│   │   ├── architecture.py      # 架构分析 Agent
│   │   ├── quality.py           # 代码质量 Agent
│   │   ├── dependency.py        # 依赖风险 Agent
│   │   └── optimization.py     # 优化建议 Agent
│   ├── graph/                  # LangGraph 工作流
│   │   ├── state.py             # SharedState 定义
│   │   └── analysis_graph.py   # 主工作流编排
│   ├── tools/                  # Agent 可用工具
│   ├── schemas/                # Pydantic 数据模型
│   │   ├── request.py
│   │   └── response.py
│   ├── main.py                 # FastAPI 入口
│   ├── pyproject.toml          # Python 依赖管理
│   └── requirements.txt
│
├── packages/
│   └── types/                  # 共享类型（前后端共用）
│       └── index.ts            # AgentEvent, AnalyzeRequest, Result 等
│
├── pnpm-workspace.yaml         # pnpm workspace 配置
├── turbo.json                  # Turborepo 流水线配置
├── package.json                # 根 package.json（workspace 元信息）
└── tsconfig.json                # 根 tsconfig（base 配置）
```

## 共享类型规范

**所有跨层类型必须放在 `packages/types/index.ts`**，禁止前后端各自定义：

- `AnalyzeRequest` — 分析请求
- `AgentEvent` — SSE 事件
- `AgentName` — Agent 名称枚举
- `AnalysisResult` — 最终分析结果
- `HistoryItem` — 历史记录

前端通过 `import { AgentEvent } from "@gitintel/types"` 使用，后端 Python 通过 `from schemas.request import AnalyzeRequest` 使用（Pydantic 模型从 `backend/schemas/` 派生）。

## 启动命令

```bash
# 安装所有依赖
pnpm install

# 启动前端开发服务器（端口 3000）
pnpm dev:frontend

# 启动后端开发服务器（端口 8000）
pnpm dev:backend

# 安装后端依赖
cd backend && pip install -r requirements.txt
```

## 命名规范

| 模块 | 命名风格 | 示例 |
|------|---------|------|
| Next.js 页面/路由 | kebab-case | `analysis-result/page.tsx` |
| React 组件 | PascalCase | `ArchitectureAgent.tsx` |
| Zustand Store | camelCase + use 前缀 | `useAnalysisStore.ts` |
| API Route | kebab-case | `route-handler.ts` |
| FastAPI Agent | snake_case | `architecture_agent.py` |
| LangGraph State | snake_case | `analysis_state.py` |
| 共享类型 | PascalCase | `AgentEvent`, `AnalyzeRequest` |
